x-common-env: &common-env
  DATABASE_URL: postgres://vinosports:vinosports@db:5432/vinosports
  REDIS_URL: redis://redis:6379/0
  SECRET_KEY: django-insecure-local-dev-only
  DEBUG: "true"
  ALLOWED_HOSTS: "*"

x-epl-build: &epl-build
  build:
    context: .
    dockerfile: apps/epl/Dockerfile
  environment:
    <<: *common-env
    DJANGO_SETTINGS_MODULE: config.settings
    DB_NAME: vinosports
    DB_USER: vinosports
    DB_PASSWORD: vinosports
    DB_HOST: db
    DB_PORT: "5432"
  depends_on:
    db:
      condition: service_healthy
    redis:
      condition: service_started

x-nba-build: &nba-build
  build:
    context: .
    dockerfile: apps/nba/Dockerfile
  environment:
    <<: *common-env
    DJANGO_SETTINGS_MODULE: config.settings
    DB_NAME: vinosports
    DB_USER: vinosports
    DB_PASSWORD: vinosports
    DB_HOST: db
    DB_PORT: "5432"
  depends_on:
    db:
      condition: service_healthy
    redis:
      condition: service_started

services:
  # --- Infrastructure ---
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vinosports
      POSTGRES_USER: vinosports
      POSTGRES_PASSWORD: vinosports
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vinosports"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  # --- Migrations (run once, shared DB) ---
  migrate:
    <<: *epl-build
    entrypoint: []
    command: >
      sh -c "
        python manage.py migrate --noinput &&
        echo 'EPL migrations complete.' &&
        cd /nba &&
        python manage.py migrate --noinput &&
        echo 'NBA migrations complete.'
      "
    volumes:
      - ./apps/nba:/nba
    restart: "no"
    profiles:
      - migrate

  # --- EPL Services ---
  epl-web:
    <<: *epl-build
    ports:
      - "8000:8000"

  epl-worker:
    <<: *epl-build
    entrypoint: []
    command: celery -A config worker -l info -Q default

  epl-beat:
    <<: *epl-build
    entrypoint: []
    command: celery -A config beat -l info

  # --- NBA Services ---
  nba-web:
    <<: *nba-build
    ports:
      - "8001:8000"

  nba-worker:
    <<: *nba-build
    entrypoint: []
    command: celery -A config worker -l info -Q default

  nba-beat:
    <<: *nba-build
    entrypoint: []
    command: celery -A config beat -l info

volumes:
  pgdata:
