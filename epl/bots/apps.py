from django.apps import AppConfig


class EplBotsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "epl.bots"
    label = "epl_bots"
    verbose_name = "EPL Bots"
