/* Payout preview — shared utility for bet form inputs */
(function () {
  // No-op placeholder; payout logic is inline in bet_form.html templates.
  // This file exists so the static reference in base.html doesn't 404.
})();
