from django.apps import AppConfig


class MatchesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "ucl.matches"
    label = "ucl_matches"
    verbose_name = "UCL Matches"
